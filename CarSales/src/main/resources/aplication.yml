spring:
  datasource:
    url: jdbc:h2:mem:sales
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always
  h2:
    console:
      enable: true
